# Note: oemer pulls onnxruntime-gpu, which falls back to CPU when no GPU is
# present (as on HF free Spaces). Do NOT also list plain onnxruntime — having
# both installed conflicts.
fastapi
uvicorn[standard]
python-multipart
oemer
opencv-python-headless
numpy
